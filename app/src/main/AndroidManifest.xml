<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto"
    package="parkhomov.andrew.lensthicknesscalculator">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ThknsCalculatorActivity"
            android:label="@string/toolbar_title_lens_thkns_calc"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.DiamCalculatorActivity"
            android:label="@string/toolbar_title_diam_calc"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.GlossaryActivity" />

        <activity
            android:name=".activities.AboutDialogActivity"
            android:theme="@style/AppThemeDialog"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".activities.SettingsActivity"
            android:noHistory="true">
        </activity>

        <activity
            android:name=".activities.LanguageActivity"
            android:noHistory="true">
        </activity>

        <activity android:name=".activities.GlossaryListActivity">
        </activity>

        <activity android:name=".activities.HelpFeedbackActivity">
        </activity>

        <activity android:name=".activities.RateAppActivity">
        </activity>

    </application>

</manifest>